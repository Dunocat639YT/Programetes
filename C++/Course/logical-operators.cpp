#include <iostream>

int main(){

    // && = check if two conditions are true
    // || = check if at least onte of two conditiona is true
    // !  = reverses the logical state of its operand


    return 0;
}